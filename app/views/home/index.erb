<!-- 들어올 때마다 랜덤하게 뜨는 방송 배너 -->
<div class="jumbotron">
  <img src="<%=@banner.url%>" width="578">
  <h1><%=@banner.title%></h1>
  <p class="lead"><%=@banner.intro%></p>
  <span class="star-rating">
    <% 1.upto(5) do |i| %>
      <% if i == @banner.rating.round(0) %>
        <i class="on"></i>
      <% else %>
        <i></i>
      <% end %>
    <% end %>
  </span>
  <p id="count"><%=@banner.rating.round(1)%> 점 (<%=@banner.comments.count%> 명)</p>
  <p><a class="btn btn-sm btn-success" href="/home/detail/<%=@banner.id%>" role="button">평가하기</a></p>
</div>
<!-- 배너끝 -->


<div class="row marketing">
  <!-- 데이터베이스에서 불러와 반복하여 뿌려줄 방송 정보 -->
  <% @tvs.each do |t| %>
  <div class="col-lg-6">
    <h4><%=t.title%></h4>
    <img src="<%=t.url%>" class="img-thumbnail" width="320">
    <p><%=t.intro%></p>
    <span class="star-rating small">
      <i></i>
      <i></i>
      <i class="on"></i>
      <i></i>
      <i></i>
    </span>
    <p id="count">4.2 점 (250 명)</p>
    <p><a class="btn btn-sm btn-success" href="/home/detail/<%=t.id%>" role="button">평가하기</a></p>
  </div>
  <% end %>
  <!-- 끝 -->
</div>
